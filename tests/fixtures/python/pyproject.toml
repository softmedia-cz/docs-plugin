[project]
name = "fixture-python-app"
version = "0.1.0"
description = "Fixture: Python e-commerce-style app for docs-plugin testing"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
